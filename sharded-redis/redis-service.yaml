apiVersion: v1
kind: Service
metadata:
  name: redis # Service name
  labels:
    apps: redis
spec:
  ports:
    - port: 6379
      name: redis
  clusterIP: None
  selector:
    app: redis # which label to find
